<h1><%= @entrepreneur.full_name %></h1>
<h2>Morada <%= @entrepreneur.address %> </h2>
<%= form_for @entrepreneur, :url => create_project_entrepreneur_path(@entrepreneur) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>
      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
    <%= f.fields_for :projects do |builder| %>
        <%= render 'project_fields', :f => builder %>
    <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
