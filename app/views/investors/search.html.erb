<p id="notice"><%= notice %></p>
<p id="notice"><%= notice %></p>


<h1>Investidores</h1>


<%= form_tag investors_search_path, :method => 'get' do %>
  <p>
    
    <%= text_field_tag :search, params[:search] %>
    <%= submit_tag "Pesquisar", :name => nil %>
  </p>
<% end %>

<table>
  <thead>
    <tr>
      <th>Nome</th>
      <th>Morada</th>
      <th>Código Postal</th>
      <th>Localidade</th>
      <th>Pais</th>
      <th>NIF</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @investors.each do |investor| %>
      <tr>
        <td><%= investor.full_name %></td>
        <td><%= investor.address %></td>
        <td><%= investor.zip_code %></td>
        <td><%= investor.town %></td>
        <td><%= investor.country %></td>
        <td><%= investor.fiscal_number %></td>
        <td><%= link_to 'Mostrar', investor %></td>
        <td><%= link_to 'Movimentar', account_movements_id_path(investor) %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>
